import 'package:flutter/material.dart';

class GroupsForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Groups form'),
        backgroundColor: Color(0xFF00CC99),
      ),
    );
  }
}
